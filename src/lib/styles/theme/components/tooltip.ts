import { defineStyleConfig } from "@chakra-ui/react";

const baseStyle = {
  borderRadius: "md",
  color: "text.main",
  padding: "8px 16px",
};
export const Tooltip = defineStyleConfig({ baseStyle });
